# Requirements - macOS Installer

- macOS with bash available.
- Wireshark installed (GUI) and user has permission to run it.
- Write access to `~/Library/Application Support/Wireshark/plugins` or `~/.local/lib/wireshark/plugins`.
- No sudo required for user-scoped install.
