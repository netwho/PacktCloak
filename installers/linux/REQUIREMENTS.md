# Requirements - Linux Installer

- Linux with bash available.
- Wireshark installed; user should ideally be in `wireshark` group for capture without sudo.
- Write access to user plugin dirs (`~/.local/lib/wireshark/plugins` preferred) or system dirs if chosen (may require sudo).
