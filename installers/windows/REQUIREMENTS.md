# Requirements - Windows Installer

- Windows with Wireshark installed (default profile under `%APPDATA%\Wireshark\plugins`).
- PowerShell available (for `install.ps1`) or cmd (for `install.bat`).
- Run the installer with user permissions that can write to `%APPDATA%\Wireshark\plugins`.
